# Copy to `.env` / `.dev.vars` as needed. Do not commit real secrets.

# Admin gate: open the board with ?admin=<this value>
VITE_ADMIN_SECRET=communal-admin

# Free hobby license for non-commercial production HTTPS deploys.
# Apply at https://tldraw.dev — local/dev works without it.
VITE_TLDRAW_LICENSE_KEY=
